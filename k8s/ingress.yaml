apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flask_hello_world
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: flask_hello_world
                port: 
                  number: 80